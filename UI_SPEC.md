# 台灣生活小助手 — UI / 功能演進規格

版本：`1.2`
狀態：開發中

## 1. 問題定義

目前功能已經完整，但入口是「功能清單 + 單次查詢表單」：使用者必須先知道要查哪一個工具，才能開始操作。新版介面要把系統變成「今天的生活工作台」，先提供摘要、捷徑與最近使用，再深入各功能。

## 2. 設計目標

- 首頁 5 秒內讓使用者知道今天的天氣、假日狀態與可做的下一步。
- 將 20 個以上的工具整理成 4 個工作區：首頁、天氣、交通、行事曆 / 生活。
- 讓常用城市、站牌與查詢保存在瀏覽器 localStorage；不保存 API key、OAuth token 或個人敏感資料。
- 跨功能資料要能並行載入、局部失敗仍可使用，不因行事曆未授權而拖垮天氣與交通。
- 桌面與手機都可用，鍵盤、焦點、錯誤與 loading 狀態清楚可見。

## 3. 主要使用流程

### F1：今日首頁

1. 首頁顯示目前日期、選定城市與摘要卡片。
2. 摘要至少包含：36 小時天氣、今日假日 / 補班、匯率快照。
3. 每張卡片都有「查看詳情」或「重新整理」，單張卡片失敗時只顯示該卡錯誤。
4. 首頁提供快捷操作：查天氣、找站牌、查車次、建立行事曆事件。

### F2：交通工作區

1. 使用者可搜尋站牌並查看去程 / 返程到站資訊。
2. 可將站牌加入收藏；收藏資料只存本機，並可在首頁顯示。
3. 收藏站牌支援 30 秒自動刷新與明確的最後更新時間。

### F3：行事曆工作區

1. 查詢、建立、修改、刪除與找空檔仍保留。
2. 破壞性刪除需二次確認；未授權時顯示「連接 Google Calendar」狀態，不影響其他工作區。
3. 建立事件表單要能從首頁快捷操作開啟，成功後顯示下一步。

### F4：個人化捷徑

- 偏好城市：預設 `台北市`，可切換並保存。
- 收藏站牌：保存 `city`、站牌名稱、UID 清單；不得保存即時結果。
- 最近查詢：最多 5 筆，僅保存查詢類型與顯示名稱。

## 4. 聚合 API 契約

新增 `GET /api/overview`：

```text
/api/overview?city=台北市&currency=USD&include_calendar=false
```

回應：

```json
{
  "city": "台北市",
  "generated_at": "2026-08-19T22:00:00+08:00",
  "cards": {
    "weather": {"status": "ok", "result": "..."},
    "holiday": {"status": "ok", "result": "..."},
    "exchange": {"status": "ok", "result": "..."},
    "calendar": {"status": "skipped", "result": null}
  }
}
```

- `weather`、`holiday`、`exchange` 必須並行查詢。
- 單一服務失敗時，該卡片為 `status: "error"`，其他卡片仍回傳。
- `include_calendar=false` 時不可觸發 Google OAuth；只有使用者主動載入行事曆才可設為 true。
- 回應不得包含 API key、OAuth token 或原始 credential。

## 5. 視覺與互動原則

- 內容優先：摘要卡、工作區、主要操作分層清楚。
- 使用台灣生活資訊適合的藍 / 青綠色系，危險操作用橘紅色，成功 / 更新用綠色。
- 所有 API 結果均經安全 Markdown / HTML 清理；外部資料不直接信任。
- loading 使用 skeleton 或明確狀態，不讓畫面看起來像沒有反應。
- mobile 使用底部主要導覽或可收合選單；桌面保留側邊導覽。

## 6. 驗收條件

- [ ] 首頁預設是今日工作台，不再直接呈現長工具清單。
- [ ] 首頁摘要透過 `/api/overview` 並行載入，單張卡片失敗不影響其他卡片。
- [ ] 城市偏好與至少一個站牌收藏可保存、讀取、刪除，重新整理後仍存在。
- [ ] 既有天氣、交通、生活資訊與 Calendar API 流程仍可從新版介面進入。
- [ ] 手機寬度與桌面寬度皆無水平溢位，主要按鈕可操作。
- [ ] 既有安全規則、離線測試、`/health` 與 CI 不回歸。

## 7. 不在本版本範圍

- 不引入需要建置步驟的前端框架；先用可部署的單一靜態入口完成體驗升級。
- 不在瀏覽器保存 Google token 或任何 API secret。
- 不自動替使用者建立、修改或刪除 Calendar 事件。
